name: CodeQL

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened
  schedule:
    - cron: "37 4 * * 1"
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  security-events: write

concurrency:
  group: codeql-${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze Swift
    runs-on: macos-26
    timeout-minutes: 45
    steps:
      - name: Checkout container-k8s
        uses: actions/checkout@v7
        with:
          path: container-k8s
          fetch-depth: 0

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: swift
          build-mode: manual
          config-file: container-k8s/.github/codeql/codeql-config.yml

      - name: Build for CodeQL
        run: make build
        working-directory: container-k8s

      - name: Analyze
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:swift"
