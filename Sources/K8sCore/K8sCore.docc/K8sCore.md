# ``K8sCore``

The reusable planning and state-management library behind the `container k8s` plugin.

`K8sCore` provides build metadata and the planned command values used to translate plugin commands into operations on the `container` runtime.

## Topics

### Command Planning

- ``PlannedCommand``

### Build Metadata

- ``K8sBuildInfo``
